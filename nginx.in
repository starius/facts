
server {

	listen	 127.0.0.1:80;
	server_name  HOST_NAME;
	access_log	/var/log/nginx/facts.access.log;

	location ~ /URL_PREFIX((img|fav|res|css).*) {
		alias DOCROOT/$1;
	}

	location /URL_PREFIX {
		fastcgi_pass   ADDRESS:PORT;
		include fastcgi_params;
	}

}

